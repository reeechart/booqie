syntax = "proto3";

import "google/protobuf/Empty.proto";

package logger;

service Logger {
    rpc LogEvent(Event) returns (google.protobuf.Empty) {}
}

message Event {
    int32 user_id = 1;
    string activity = 2;
}
